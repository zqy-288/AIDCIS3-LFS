# ==============================================================================
# 工业内窥镜图像处理系统 - 依赖包列表
# ==============================================================================

# 核心计算库
numpy>=1.21.0,<2.1.0           # 数值计算和数组操作
scipy>=1.7.0,<1.14.0           # 科学计算库（插值、信号处理）

# 计算机视觉和图像处理
opencv-python>=4.5.3,<4.12.0   # 计算机视觉核心库
scikit-image>=0.18.3,<0.25.0   # 图像处理算法库
Pillow>=8.3.2,<12.0.0          # 图像文件读写

# 深度学习框架（用于高级图像处理）
torch>=1.9.0,<2.8.0            # PyTorch深度学习框架
torchvision>=0.10.0,<0.23.0    # 计算机视觉模型和变换

# 数据可视化
matplotlib>=3.4.3,<4.0.0       # 绘图和可视化

# 进度条和用户界面
tqdm>=4.62.3,<5.0.0            # 进度条显示

# 可选依赖（性能优化）
# 注意：以下库可能需要特定的安装条件

# CUDA支持（如果有GPU）
# torch-audio>=0.10.0           # 音频处理（可选）

# 高性能计算（可选）
# opencv-contrib-python>=4.5.3  # OpenCV额外模块
# numba>=0.56.0                 # JIT编译加速

# ==============================================================================
# 安装说明：
# 1. 基础安装：pip install -r requirements.txt
# 2. GPU支持：安装对应CUDA版本的PyTorch
# 3. 性能优化：可选安装上述注释的包
# ============================================================================== 